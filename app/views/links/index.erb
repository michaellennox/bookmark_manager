<h1>Add new link</h1>
<ul id='links'>
    <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tags: <%= link.tags.map(&:tags).join("") %>
    </li>
    <% end %>
</ul>
